export type Size = {
  width: number;
  height: number;
};

export type Range = {
  start: number;
  end: number;
};

export type TimelineItemData = {
  time: {
    start: number;
    end: number;
  };
};
