export type Size = {
  width: number;
  height: number;
};

export type TimeRange = {
  start: number;
  end: number;
};

export type NumberBounds = {
  min: number;
  max: number;
};

export type TimelineItemData = {
  time: {
    start: number;
    end: number;
  };
};
